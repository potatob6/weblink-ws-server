{
  "name": "wbelink-ws-server",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch ./src/index.ts",
    "start": "bun ./src/index.ts",
    "build": "bun build src/index.ts --target bun --outdir dist"
  },
  "dependencies": {
    "bun": "1.1.30",
    "ioredis": "^5.4.1",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.11",
    "pino-pretty": "^11.3.0"
  }
}
